<Page
    x:Class="Bti.Babble.Metro.CommentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bti.Babble.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Comment" Style="{StaticResource HeaderTextStyle}" Margin="40,20" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0, 10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <Image Source="{Binding ImageSource}" Width="48" Height="48"  />
                <TextBlock Text="{Binding User}" Margin="5,5,0,0" Style="{StaticResource SubheaderTextStyle}" />
            </StackPanel>
            <TextBlock Text="{Binding Message}" Style="{StaticResource ItemTextStyle}" Margin="10,0,0,0" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <TextBlock Style="{StaticResource SubheaderTextStyle}" Text="Respond" HorizontalAlignment="Center" />
                <TextBox FontSize="18" Width="400" Height="200" Margin="20,0,0,0" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="110,10,0,0">
                <Button>
                    <TextBlock Text="Submit"/>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>