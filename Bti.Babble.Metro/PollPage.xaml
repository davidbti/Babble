<Page
    x:Class="Bti.Babble.Metro.PollPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bti.Babble.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Poll" Style="{StaticResource HeaderTextStyle}" Margin="40,20" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Image x:Name="PollImage" Height="350" Width="700" Source="{Binding PollImageSource}" />
            <TextBlock Style="{StaticResource SubheaderTextStyle}" Text="{Binding Question}" 
                       TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,20,0,0" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button>
                    <TextBlock Text="Yes"/>
                </Button>
                <Button Margin="20,0,0,0">
                    <TextBlock Text="No"/>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>