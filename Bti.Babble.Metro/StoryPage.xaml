<Page
    x:Class="Bti.Babble.Metro.StoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bti.Babble.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Story" Style="{StaticResource PageHeaderTextStyle}" Margin="40" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0, 10">
            <TextBlock HorizontalAlignment="Center" Style="{StaticResource SubheaderTextStyle}" Text="{Binding Title}" TextWrapping="Wrap" />
            <HyperlinkButton x:Name="ReadMore" Tag="{Binding Link}" Click="ReadMore_Click" Margin="0,10,0,0">
                <Image Source="{Binding StoryImage}" Stretch="UniformToFill" Width="950" Height="534"/>
            </HyperlinkButton>
            <TextBlock HorizontalAlignment="Left" Style="{StaticResource ItemTextStyle}" Text="{Binding Description}" Margin="0,10,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>