<Page
    x:Class="Bti.Babble.Metro.StoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bti.Babble.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <StackPanel Margin="100,100,100,100">
            <TextBlock FontSize="42" Text="{Binding Title}" TextWrapping="Wrap" />
            <Image Source="{Binding StoryImage}" Margin="0,10,0,0" />
            <TextBlock FontSize="24" Text="{Binding Description}" Margin="0,10,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>