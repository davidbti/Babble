<Page
    x:Class="Bti.Babble.Metro.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bti.Babble.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Info" Style="{StaticResource PageHeaderTextStyle}" Margin="40" />

        <Grid Grid.Row="1" Background="{StaticResource ApplicationPageBackgroundBrush}">
            <WebView x:Name="ContentView" />
        </Grid>
    </Grid>
</Page>